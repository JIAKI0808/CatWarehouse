"""API 分区规则（2026-09-14 起，承合同第 9 条「后端接口必须分区管理」）。

**一个功能分区 = 一个文件夹**，分区内每个资源一个模块，分区入口固定叫 `router.py`。

| 分区 | 职责 | 承载的资源 |
| --- | --- | --- |
| `api.inventory` | 库存 | 大类、子分类、明细条目、标签 |
| `api.pricing` | 售价 | 售价记录、售价分类/子分类 |
| `api.finance` | 财务 | 账本流水、预算、定时账单 |
| `api.analytics` | 分析 | 概览、分类统计、趋势、月度对比 |
| `api.intake` | 录入 | 单据上传、票据 OCR、语音录入 |
| `api.notify` | 通知 | 系统通知、库存预警 |
| `api.system` | 系统 | 设置、备份、币种 |

约定（违反任何一条都会改掉 `/openapi.json`，见 `api/router.py` 的说明）：

1. **分区聚合器不带 prefix、不带 tags** —— 子路由自带完整路径与标签。
2. **文件名不变**（仍叫 `category_router.py` 而不是 `category.py`）—— 保住 `git log --follow`
   的可回溯性，也让人一眼看出它是从旧的平铺结构搬过来的。
3. **分区之间不互相 import** —— 一个分区坏掉不该拖垮另一个。
4. 本文件（`api/__init__.py`）只写规则，**不 import 任何子模块**。
"""
